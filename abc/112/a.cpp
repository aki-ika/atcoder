#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

#define rep(i,n) for(ll i=0;i<ll(n);i++)
#define bit(n) (1LL<<(n))
#define itn int
#define vi vector<int>

signed main() {
  int n;
  cin >> n;
  if(n == 2) {
    int a,b;
    cin >> a >> b;
    cout << a+b << endl;
  }else{
    cout << "Hello World" << endl;
  }

}
